# Godot
.godot/
*.tmp
export_presets.cfg.secrets

# Android Gradle build template (regenerable; we ship via the prebuilt-template path)
/android/

# Build output (APKs etc. ship via GitHub releases, not git)
build/
*.apk
*.aab

# Test artifacts
*.log
test-results.xml

# OS
Thumbs.db
.DS_Store
/reports/
